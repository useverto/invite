{
  "scripts": {
    "build": "node ./scripts/build.js",
    "deploy": "node ./scripts/deploy.js",
    "fmt": "prettier --write ."
  },
  "devDependencies": {
    "arweave": "^1.10.14",
    "esbuild": "^0.12.2",
    "prettier": "^2.3.0",
    "smartweave": "^0.4.30",
    "typescript": "^4.2.4"
  }
}
